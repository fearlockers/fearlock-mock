'use client'

export default function Header() {
  return (
    <div className="sticky top-0 z-40 flex h-12 shrink-0 items-center justify-between bg-gray-100 dark:bg-gray-900 px-3 sm:px-4 lg:px-6">
      {/* Left side - could add breadcrumbs or search here */}
      <div className="flex items-center">
        {/* Placeholder for future content */}
        </div>

      {/* Right side - could add notifications or other controls here */}
      <div className="flex items-center">
        {/* Placeholder for future content like notifications */}
      </div>
    </div>
  )
} 